WP:

Graph

Map<title, Category>
Map<title, Page>

/*	Esto haria posible modificar 
	una cat/pag!! Queremos eso?
	
	Creo que no porque si solo tienen
	título si se cambia sin querer se
	lia parda en el MAP
*/
Category getCategory(String title)
Page getPage(String title)

// Igual que los getters, peligroso...
ArrayList<Category> getSuperC(Category c)
ArrayList<Category> getSubC(Category c)

ArrayList<Page> getCP(Category c)
ArrayList<Category> getPC(Page c)


void addCategory(Category c)
void addPage(Page p)

void delCategory(Category c)
void delPage(Page p)

bool catExists(String title)
bool pagExists(String title)

void setElementTitle(Element e, String title)

String getCategoryTitle(Category c)
String getPageTitle(Page p)

/*	La implementación del grafo no permite hacer esto
	ya que addEdge añade en los dos sentidos.
	
	Hasta que no hagamos nuestra subclase grafo
	no podremos indicar que tipo de enlace es
	
	El contructor de Edge requiere un peso inicial
	cuando realmente se calcula una vez dado el grafo???
	
	El calculo del peso se hace durante el algoritmo
	o una vez dado el grafo???? Mejor durante el algoritmo no?
*/
void addSuperLink(Category c1, Category c2)
void addSubLink(Category c1, Category c2)
void addCPLink(Category c, Page p)
void addPCLink(Page p, Category c)



Element

title

void setTitle(String title)
String getTitle()


Community Algorithm

